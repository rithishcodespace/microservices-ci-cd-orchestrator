# name: Run Tests

# on:
#   push:
#     branches:
#       - main
#     paths:
#       - services/**
#   pull_request:
#   workflow_dispatch:

# jobs:
#   filter:
#     runs-on: ubuntu-24.04
#     outputs:
#       services: ${{ steps.filter.outputs.changes }}
#     steps:
#       - uses: actions/checkout@ff7abcd0c3c05ccf6adc123a8cd1fd4fb30fb493 # v5.0.0
#       - name: Paths Changes Filter
#         id: filter
#         uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3.0.2
#         with:
#           token: ${{ secrets.GITHUB_TOKEN }}
#           filters: .github/utils/file-filters.yaml

#   run-tests:
#     runs-on: ubuntu-24.04
#     if: ${{ needs.filter.outputs.services != '[]' && needs.filter.outputs.services != '' }}
#     needs: filter
#     strategy:
#       matrix:
#         service: ${{ fromJson(needs.filter.outputs.services) }}
#       fail-fast: false
#     steps:
#       - uses: actions/checkout@ff7abcd0c3c05ccf6adc123a8cd1fd4fb30fb493 # v5.0.0
#       - uses: ./.github/actions/setup-dependencies
#         with:
#           install_task: "true"
#           install_go: ${{ startsWith(matrix.service, 'services/go/') }}
#           install_node: ${{ startsWith(matrix.service, 'services/node/') || startsWith(matrix.service, 'react/') }}
#           install_python: ${{ startsWith(matrix.service, 'services/python/') }}
#           service_path: ${{ matrix.service }}
#       - name: Install / Build
#         working-directory: ${{ matrix.service }}
#         run: |
#           task install-ci
#       - name: run tests
#         working-directory: ${{ matrix.service }}
#         run: |
#           task test

#   # This job gets skipped if all upstream jobs succeeded, but it runs (and fails) if any upstream failed.
#   # This is what we want since GitHub required actions do not block merging when skipped, but can block it when failed.
#   # See: https://github.com/actions/runner/issues/2566#issuecomment-3053484216
#   run-tests-failure-alert:
#     runs-on: ubuntu-latest
#     needs: [filter, run-tests]
#     if: ${{ cancelled() || contains(needs.*.result, 'cancelled') || contains(needs.*.result, 'failure') }}
#     steps:
#       - name: Conditionally Fail Required Check
#         shell: bash
#         run: |
#           echo "::error Some required job has failed!"
#           exit 1

name: Run Tests

on:
  workflow_dispatch:

jobs:

  filter:
    runs-on: ubuntu:24.04
    outputs:
      services: ${{ steps.filter.outputs.changes }} # This jobs output will be named as (changes)
    steps:
      - name: filter
        id: filter
        uses: dorny/paths-filter@v3 # It checks which files/folders changed in a commit or PR.
        with:
          token: ${{ secrets.GITHUB_TOKEN }} # for dorny to access gihtub api
          filters: .github/utils/file-filters.yaml

  test:
    runs-on: ubuntu-24.04
    needs: filter
    strategy:
      fail-fast: false
      matrix:
        service:
          [
            "services/node/api-node",
            "services/go/api-golang",
            "services/react/client-react",
            "services/python/load-generator-python"
          ]

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        if: ${{ startsWith(matrix.service,'services/node/') || startsWith(matrix.service,'services/react/') }}
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: ${{ matrix.service }}/package-lock.json

      - name: Setup Go
        if: ${{ startsWith(matrix.service,'services/go/') }}
        uses: actions/setup-go@v4
        with:
          go-version: '1.22'
          cache: true

      - name: Setup Python
        if: ${{ startsWith(matrix.service,'services/python/') }}
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'poetry'
          cache-dependency-path: ${{ matrix.service }}/poetry.lock

      - name: Install Poetry
        if: ${{ startsWith(matrix.service,'services/python/') }}
        uses: snok/install-poetry@v1

      - name: Install Task CLI
        uses: supplypike/setup-bin@v1
        with:
          uri: https://github.com/go-task/task/releases/download/v3.44.1/task_linux_amd64.tar.gz
          name: task
          version: v3.44.1

      - name: Install dependencies
        working-directory: ${{ matrix.service }}
        run: |
          task install-ci

      - name: Run tests
        working-directory: ${{ matrix.service }}
        run: |
          task test